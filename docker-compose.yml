version: "3.8"

services:
  web:
    build: .
    ports:
      - "8050:8050"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./reports:/app/reports
      - ./promises.csv:/app/promises.csv
    restart: unless-stopped
